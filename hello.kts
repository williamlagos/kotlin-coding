/**
 * You can edit, run, and share this code. 
 * play.kotlinlang.org 
 */

fun printMessage(message: String) {
    println(message)
}

fun returnValues(): Int {
    return 0
}

println("Hello, world!")

var a = "Text"
var number = 20

var months = arrayOf("January", "February", "March")

println("$a + $number")
println(months[1])

printMessage(months[2])
println(returnValues())